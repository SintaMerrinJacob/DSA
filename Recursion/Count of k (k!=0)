int countZeros(int n , int k) 
{
  
   if(n==0)
      return 0;
    
    int digit = n%10;
    cout<<"Digit is "<<digit<<endl;
    
    if(digit == k)
    {
        return countZeros(n/10)+1;
    }
    
    return countZeros(n/10);

      

      return ans;
}
